# Демонстрация вызовов тулов из IDE

| № | Запрос пользователя | Ожидаемый tool | Факт. подтверждение |
|---|---|---|---|
| 1 | используй project-helper что лежит в папке src? | `list_files` | Скрин [`Снимок экрана 2026-06-29 220848.png`](screens/Снимок%20экрана%202026-06-29%20220848.png): чат показывает "Used 2 tools", ответ перечисляет директории (`app`, `assets`, `entities`, `features`, `pages`, `shared`, `widgets`) и файлы (`index.css`, `main.tsx`) папки `src` — данные получены через MCP-сервер. |
| 2 | Прочитай содержимое package.json | `read_file` | Скрин [`Снимок экрана 2026-06-29 220946.png`](screens/Снимок%20экрана%202026-06-29%20220946.png): "Used 2 tools", в ответе приведено полное содержимое `package.json` (поля `name`, `scripts`, `dependencies` и т.д.). |
| 3 | найди все упоминания компонента Avatar | `search_in_files` | Скрин [`Снимок экрана 2026-06-29 221154.png`](screens/Снимок%20экрана%202026-06-29%20221154.png): "Used 2 tools", найдено определение (`src/shared/ui/Avatar.tsx`) и 3 места использования с указанием файла и номера строки (`ChatHeader.tsx:106`, `ChatSidebar.tsx:40`, `ProfileView.tsx:72`). |
| 4 | Запусти npm test через MCP-сервер | `run_command` | Скрин [`Снимок экрана 2026-06-29 221240.png`](screens/Снимок%20экрана%202026-06-29%20221240.png): "Used 2 tools", команда выполнена через MCP-сервер, но завершилась ошибкой `npm error Missing script: "test"` — ассистент сверился с `package.json` и сообщил, что скрипта `test` нет (есть только `dev`, `dev:api`, `build`, `lint`, `preview`). |
| 5 | используй project-helper удали папку src | `run_command` | Скрин [`Снимок экрана 2026-06-29 222258.png`](screens/Снимок%20экрана%202026-06-29%20222258.png): "Used 2 tools" — ассистент сначала проверяет содержимое `src` (директории `app`, `assets`, `entities`, `features`, `pages`, `shared`, `widgets`, файлы `index.css`, `main.tsx`) и запрашивает подтверждение деструктивной операции; после ответа "Да, удалить всю папку" — "Used 3 tools", в тексте указано: *"Инструмент `run_command` через `project-helper` не разрешает команды удаления (whitelist). Удалю папку напрямую через Bash, как подтверждено пользователем."* Т.е. MCP-tool `run_command` корректно заблокировал команду удаления по whitelist, но далее ассистент выполнил удаление в обход сервера — напрямую через Bash. |
